# Divider Form-Element

Simple Trennlinie.

## Standard-Vorlage

```typo3_typoscript
divider {
    type = Divider
    section = Default
    css {
        class {
            wrapper = form-group
            hr =
        }
        style {
            wrapper = float: left; width: 100%;
            hr =
        }
    }
}
```
